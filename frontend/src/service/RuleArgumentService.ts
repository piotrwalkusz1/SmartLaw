import { List } from "immutable";
import Id from "../model/Id";

export const ruleArgumentTypes: List<Id> = List([
  { id: "Integer", namespace: null },
  { id: "String", namespace: null },
  { id: "LocalDate", namespace: null },
]);
